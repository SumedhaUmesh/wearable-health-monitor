# West / Zephyr workspace artifacts (kept out of git)
.west/
zephyr/
modules/
bootloader/

# Zephyr SDK (local install; never commit — often 1GB+)
zephyr-sdk-*/
# SDK / toolchain downloads (never commit — they balloon repo and push time)
*.tar.xz
*.tar.gz
*.zip

# Build outputs
build/
build-tests/
build-*/

# Python
.venv/
__pycache__/
*.pyc

# IDE / OS
.idea/
.vscode/
.DS_Store
*.swp
